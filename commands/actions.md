---
name: actions
description: Discover, inspect, or safely invoke Portal actions
argument-hint: [action-id]
---

The canonical action instructions live in
`${CLAUDE_PLUGIN_ROOT}/skills/actions/SKILL.md`.

Before doing anything else, use the Read tool to read that file and follow its
instructions for this command.
