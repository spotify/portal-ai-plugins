---
name: setup
description: Set up Spotify Portal CLI and supported MCP access
argument-hint: [instance-name or backend-url]
---

The canonical setup instructions live in
`${CLAUDE_PLUGIN_ROOT}/skills/setup/SKILL.md`.

Before doing anything else, use the Read tool to read that file and follow its
instructions for this command.
